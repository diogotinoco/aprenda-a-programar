let contador = 10
let n = 10
let i = 1

while (i <= n) {
    let resultado = contador * i
    let textodeexibicao = `${contador} X ${i} = ${resultado}` 
    console.log(textodeexibicao)
    i = i + 1
}
